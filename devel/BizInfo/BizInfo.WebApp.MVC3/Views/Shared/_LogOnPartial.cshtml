@if (Request.IsAuthenticated)
{
    <text>Uživatel <strong>@User.Identity.Name</strong>
        [ @Html.ActionLink("Odhlásit", "LogOff", "Account") ]</text>
}
else
{
    @:[ @Html.ActionLink("Přihlásit", "LogOn", "Account") ]
}
