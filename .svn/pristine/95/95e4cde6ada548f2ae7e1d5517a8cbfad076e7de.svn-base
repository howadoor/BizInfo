using BizInfo.Model.Entities;

namespace BizInfo.WebApp.MVC3.Data
{
    public static class DataSource
    {
        public static BizInfoModelContainer container;

        public static BizInfoModelContainer Container
        {
            get
            {
                if (container == null) container = new BizInfoModelContainer();
                return container;
            }
        }
    }
}